package br.senai.sp.jandira.model;

public class Conta {
	
	// Atributo da classe ou Propriedade da classe
	private String tipo;
	private double saldo;
	public String numero;
	public String titular;
	public boolean ativa;
	public double chequeEspecial;
	
	//*** Métodos de acesso aos atributos da classe
	// *** getters and setters
	
	public void setTipo(String tipo) {
	//parâmetro
       
	     //O método equals é utilizado para comparações.
		if(tipo.equals("Corrente") || tipo.equals("Poupança") ) {
			//o this serve para diferenciar quando o método tem o mesmo nome de parametro que a class
			this.tipo = tipo;		
		}else {
			System.out.println("O tipo deve ser Corrente ou Poupança");
		}
		
		
	}
	
	// Métodos diz o que um objeto faz
	public void depositar(double valorDeposito) {
		//parâmetro
		if (valorDeposito < 0) {
			System.out.println("Valor inválido!!!");
		} else {
			saldo += valorDeposito;
		}
		
	}
	
	public void mostrarSaldoDaConta() {
		System.out.println("Saldo: " + saldo);
		System.out.println("Saldo + Limite: " + (saldo + chequeEspecial));
	}
	
	public double getSaldo(){
		return saldo;
	}
	
	public void sacar(double valorDoSaque) {
		//parâmetro

		
		if ((saldo + chequeEspecial) >= valorDoSaque) {
			saldo -= valorDoSaque;
		} else {
			System.out.println("Saldo insuficiente!");
		}
		
		
	}
	
	public void transferir() {
		
	}

}


